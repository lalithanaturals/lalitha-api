# pb_hooks

Reserved for PocketBase JS server-side hooks (e.g. `onRecordBeforeCreateRequest` for the
`exchange_records.display_id` sequential-id logic described in scrap-calc's PROJECT_PLAN.md, once
that module is migrated). Empty for now — the Print module's collections don't need any hooks
beyond the declarative API rules already defined in `pb_migrations/`.
